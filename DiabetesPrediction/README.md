This code uses  Support Vector Machine model to predict whether a person has diabetes or not. 

We can enter the features in input section of the last cell, which tell us if a person has diabetes or not.
